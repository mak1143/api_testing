[project]
name = "api_testing"
version = "0.1.0"
description = "Scripts for testing and visualizing API response data"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "plotly>=6.9.0",
    "requests>=2.34.2",
]
